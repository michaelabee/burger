
	<li>
		{{burger.burger_name}}

		<button class="btn btn-light" id="change-devoured"  data-id="{{id}}" data-newDevoured="{{devoured}}">
			{{#if burger.devoured}}Yum!{{else}}Devour it!{{/if}}
		</button>

	</li>
